// Placeholder for real Google Calendar sync. See ./README.md.

/**
 * @param {{ title: string, startIso: string, notes?: string }} event
 * @returns {Promise<{ id: string }>}
 */
export async function createEvent(event) { // eslint-disable-line no-unused-vars
  throw new Error('NOT_IMPLEMENTED: Google Calendar integration is not connected yet.')
}
